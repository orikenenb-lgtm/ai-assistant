# ===== Kerem Orders — Frontend =====
# העתק קובץ זה ל-.env ומלא ערך אחד בלבד.

# כתובת ה-API של הבקנד (Railway). זה המשתנה היחיד שהפרונט צריך —
# כל הגישה ל-Supabase עוברת דרך הבקנד, לא ישירות מהדפדפן.
VITE_API_URL=http://localhost:8000
